#include <iostream>
#include<cstring>

using namespace std;
int main()
{
	string S;
	cin >> S;
	int j = 0,k = 0;
	for (int i = 0; i < sizeof(S); i++)
	{
		if (S[i] == '#')
		{
			j++;

		}
		else if (S[i] == '*')
		{
			k++;
		}
	}
	cout << k - j;
}
